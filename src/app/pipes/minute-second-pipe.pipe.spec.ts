import { MinuteSecondPipePipe } from './minute-second-pipe.pipe';

describe('MinuteSecondPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MinuteSecondPipePipe();
    expect(pipe).toBeTruthy();
  });
});
